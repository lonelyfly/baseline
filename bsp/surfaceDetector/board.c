#include "board.h"


/**
 * @brief Initializes the board.
 *
 * This function initializes the board specific hardware. Should be called
 * once at startup. It is called from the main() function.
 */
bool board_init(void)
{
    
}